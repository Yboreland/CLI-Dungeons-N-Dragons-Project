public class WuTangClan extends Characters {
    public WuTangClan(String name, int maxHealth, int attack, int def, int speed) {
        this.setMaxHealth(maxHealth);
        this.setAttack(attack);
        this.setDefense(def);
        this.setSpeed(speed);
        this.setName(name);


    }
}
